import MobileScannerPage from "@/components/scanner/MobileScanner";

export const metadata = {
    title: "Urovo RT40 Scanner | Navbahor ERP",
    description: "Industrial Mobile Scanning Terminal Interface",
    viewport: "width=device-width, initial-scale=1, maximum-scale=1, user-scalable=0",
};

export default function Page() {
    return <MobileScannerPage />;
}
